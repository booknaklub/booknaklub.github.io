function readInpute(){
    outer :while(true){
        let inputNum = prompt("Enter an integer to quit");
        let allNum = [];
        if (inputNum >= 0 && inputNum % 1 == 0 ){
            allNum.push(inputNum);
        } else if (inputNum<0) {
            allNum.sort
            let minNum = allNum[0];
            let maxNum = allNum[];
        }
    }
    
}