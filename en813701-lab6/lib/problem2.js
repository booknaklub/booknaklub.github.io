function readInpute(){
    outer :while(true){
        let inputNum = prompt("Enter an integer to quit");
        let allNum = [];
        let minNum;
        let maxNum;
        let averageNum;
        let totalNum = 0;
        if (inputNum >= 0 && inputNum % 1 == 0 ){
            allNum.push(inputNum);
            totalNum +=inputNum;
        } else if (inputNum<0) {
            allNum.sort
            minNum = allNum[0];
            let maxNum = allNum[allNum-1];
            let averageNum = totalNum/
        }
    }
    
}